(define-structure
  svm svm-interface
  (open scsh-user
        srfi-6
        define-record-types)
  (files opt utils
         docker
         emacs vim
         brunch luminus descjop composer sencha
         rsh esh upg
         svn
         os lnmp ins
         svm))
